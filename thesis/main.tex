\documentclass[a4paper,onecolumn,11pt,unpublished]{quantumarticle}
\pdfoutput=1
\usepackage[numbers,sort&compress]{natbib} %bibliography: numeric [1]-style citations
\usepackage[nottoc]{tocbibind} %biblio in TOC
\usepackage[printonlyused,withpage]{acronym} %acronyms list with page of first use
\usepackage{minted} %to insert colored and tabbed code

\usepackage{algorithm}%for pseudo-code
\usepackage[noend]{algpseudocode}

\usepackage{braket} %bras, kets and other quantum notation

\usepackage[utf8]{inputenc}  %Input what you want e.g., √©, ≈Ç, a, √º
\usepackage[T1]{fontenc}     %Output what you want e.g., √©, ≈Ç, a, √º
\usepackage[english]{babel}  %Do hyphenation according to british english
\hyphenation{bi-or-thog-o-nal}
%\usepackage[sc,osf]{mathpazo}\linespread{1.05}  %Palatino font
  % URL font that go well wtih palatino
%\usepackage[scaled=1.03]{inconsolata} %Monospace font
\usepackage{hyperref}  %Hyperlinks (pink, green, blue)
\usepackage{graphicx} % Package to insert external figures
\usepackage{flafter} % Prevent floats from appearing before their source position
\usepackage{placeins} % Provide \FloatBarrier at section boundaries
\usepackage[babel]{microtype}  %Improves text justification

\usepackage{amsmath,amssymb,amsthm,bm,amsfonts,mathrsfs} %Useful math packages
\newtheorem{theorem}{Theorem}

%extras
\usepackage{wrapfig}
\usepackage{physics}
\usepackage{bbold}

\usepackage{tikz}
\usetikzlibrary{automata,arrows.meta,positioning,patterns}
\usepackage{standalone}

% placeholder for a number awaiting the production cluster run
\newcommand{\OPEN}{\ensuremath{\mathrm{[OPEN]}}}

\begin{document}

\pagenumbering{Roman}   % front matter: Roman pages; switches to arabic at the Introduction

% Cover page, mimicking the master's-programme template. Unnumbered, absent from the table of
% contents, and the page counter is reset after it so it does not count towards the total.
\begin{titlepage}
  \thispagestyle{empty}
  \centering

  \vspace*{\fill}

  \includegraphics[width=0.50\textwidth]{imgs/logo_left.png}\\[2.2cm]

  {\huge\scshape Master in Quantum Science and Technology}\\[0.35cm]
  {\LARGE Master's thesis}\\[2.0cm]

  \rule{\linewidth}{0.5pt}\\[0.55cm]
  {\Huge\bfseries Universal properties from\\[0.25cm] quantum many-body dynamics\par}
  \vspace{0.55cm}
  \rule{\linewidth}{0.5pt}\\[1.9cm]

  {\LARGE Joaqu\'in G. M\'arquez Olgu\'in}\\[0.9cm]
  {\Large\textit{Supervisor: Stefano Carignano}}

  \vspace*{\fill}

  \includegraphics[width=0.70\textwidth]{imgs/logo_right.png}

\end{titlepage}
\setcounter{page}{1}

\title[Universal properties from quantum many-body dynamics]{Universal properties from quantum many-body dynamics}

\author{Joaqu\'in G. M\'arquez Olgu\'in}
\supervisor{Stefano Carignano}

\address{Barcelona Supercomputing Center, Plaça Eusebi Güell, 08034 Barcelona}
\ead{jmarquol30@alumnes.ub.edu}
\date{Aug 25, 2026}


\begin{abstract}
  The Loschmidt echo---the amplitude for a quantum state to return to itself---provides a route to the universal properties of a critical many-body system. Continued from real to imaginary time, it becomes the partition function of a conformal field theory on a strip, and its universal data---the central charge and the operator content---are encoded in the spectrum of a transfer matrix, obtained by contracting the space--time tensor network along the space direction. We apply this framework to a transverse-field Ising model extended by a next-nearest-neighbour coupling: a non-integrable chain that remains critical and in the Ising universality class over a wide range of that coupling. After a quench to the critical point, the temporal entanglement that governs the cost of the transverse contraction grows only logarithmically with the evolution time, so the method remains efficient at times where conventional evolution algorithms are limited by the entanglement barrier. We develop a translation-invariant matrix product operator, accurate to second order in the time step, representing the time evolution of the model, together with a block power method that resolves the leading spectrum of the resulting non-Hermitian transfer matrix. Where the method converges, we recover the central charge and operator content predicted by the Ising universality class.
\end{abstract}

\noindent{\it Keywords}: Loschmidt Echo, Quantum Quench, Tensor Networks, Transverse Contraction, Temporal Entanglement, Conformal Field Theory, Criticality


\clearpage

\FloatBarrier
\section*{Acknowledgements}

\emph{Lo primero de todo, quiero agradecer a mi familia, a la que llevo siempre en el corazón a pesar de la dura distancia. Pero en especial a ti, mami, por ser el mejor ejemplo a seguir que he podido encontrar en esta vida. Ninguno de mis logros lo podría haber conseguido sin haber conocido antes lo que es ser una persona buena, perseverante y resiliente como lo eres tú.}

\emph{También quiero mencionar a la gente con la que he compartido oficina en el BSC: gracias Stefano por darme la posibilidad de realizar este proyecto, y gracias en especial a César y Pau, por haberme apoyado en todo lo que he necesitado y por haber hecho de los días que he estado aquí algo mejor, a pesar de las dificultades.}

\emph{Luego está la familia que uno elige. Gracias a los que me acompañaron durante mis años de carrera: a los ``coopencos'', a los ``zumeros'', y a mi gente en Almería, en Madrid y en Los Ángeles. Gracias a los nuevos amigos que he hecho durante este último año en Barcelona: a los compis del máster, y a todos aquellos con los que he asistido y sobrevivido a hackathons y schools por toda Europa a través del maravilloso programa DigiQ.}

\emph{Gracias a mis compis de piso, Jesús y Miguel, por hacer de Barcelona un lugar más cercano a un hogar, por las interminables noches de chachara, chistes y videojuegos, y por siempre apoyarme y sacarme una sonrisa cuando más lo he necesitado.}

\emph{Y por último y más importante, GRACIAS a ti, Helena, por haber sido mi soporte constante en todas las etapas de esta última etapa de mi vida. Sentir tu cariño y la bonita energía que radias es algo que no tiene precio, y de lo que me siento muy, muy afortunado. Gracias por siempre confiar en mí y por cuidarme como sólo tú lo haces.}



\vspace{2\baselineskip}

\section*{Note on the use of Artificial Intelligence (AI)}
Generative AI tools were used during the preparation of this thesis. Anthropic's Claude models assisted with brainstorming, preliminary drafts, debugging numerical code, and automating repetitive tasks, including the production of figures and diagrams. OpenAI's ChatGPT and Google's Gemini assisted with the organisation and clarity of the text and with identifying typographical errors. All AI-assisted material was reviewed, verified, and revised by the author, who takes full responsibility for the content of the thesis.

\clearpage

\tableofcontents

\clearpage

\FloatBarrier
\section*{Acronyms}
\begin{acronym}[DMRG]
  \acro{CFT}{Conformal Field Theory}
  \acro{DMRG}{Density Matrix Renormalization Group}
  \acro{FSM}{finite-state machine}
  \acro{JW}{Jordan--Wigner}
  \acro{MPO}{Matrix Product Operator}
  \acro{MPS}{Matrix Product State}
  \acro{NN}{nearest-neighbour}
  \acro{NNN}{next-nearest-neighbour}
  \acro{PEPS}{Projected Entangled-Pair State}
  \acro{RDM}{Reduced Density Matrix}
  \acro{RTM}{Reduced Transition Matrix}
  \acro{SVD}{Singular Value Decomposition}
  \acro{TN}{Tensor Network}
  \acro{tMPO}{temporal Matrix Product Operator}
  \acro{tMPS}{temporal Matrix Product State}
\end{acronym}

\clearpage
\pagenumbering{arabic}

\FloatBarrier
\section{Introduction and Motivation}
\label{sec:intro}

Understanding the evolution of a closed quantum system driven far from equilibrium is a central problem in modern many-body physics. Although such dynamics are difficult to simulate, they can reveal universal information at a critical point. In particular, the real-time evolution of a simple initial state can encode the central charge and operator content of the underlying conformal field theory~\cite{carignano_tagliacozzo2025}, even though these are equilibrium properties of the low-energy spectrum, normally extracted from the ground state. This route is especially valuable for non-integrable models, where analytical results are scarce.

Exact numerical simulations are severely limited by the exponential scaling of the Hilbert space, which makes representing a generic many-body wavefunction intractable. To circumvent this bottleneck, the community has turned to \acp{TN}, a framework that efficiently compresses quantum states by using entanglement as the organising principle. For one-dimensional systems, \acp{MPS} combined with algorithms such as the \ac{DMRG} have become the state-of-the-art approach for computing ground states~\cite{white1992,schollwock2011,banuls2023}. Their efficiency comes from the area law of entanglement, which holds for ground states of one-dimensional systems with local gapped Hamiltonians: the entanglement entropy across a bipartition saturates to a constant rather than growing with the subsystem size~\cite{hastings2007}. Since the required \ac{MPS} bond dimension is controlled by this entanglement, the ansatz naturally targets the low-entanglement corner of Hilbert space where these ground states lie.

Simulating dynamics, however, runs into the so-called entanglement barrier~\cite{reid2021,carignano2025sampled}. After a quantum quench, the entanglement entropy typically grows linearly in time, approaching volume-law behaviour~\cite{calabrese_cardy2005}. Although highly optimised algorithms such as time-evolving block decimation (TEBD)~\cite{vidal2004} and the time-dependent variational principle (TDVP)~\cite{haegeman2011} are effective at short times, capturing this growth requires a bond dimension that increases exponentially with time, making conventional simulations intractable at late times~\cite{schuch2008,paeckel2019}.

Transverse contraction~\cite{banuls2009,muller_hermes2012,lerose2023} tackles this problem by adopting a different perspective: rather than evolving a state forward in time as in the Schr\"odinger picture, it represents the full dynamics as a two-dimensional tensor network and contracts it along the original spatial direction. The computational cost is then controlled by the temporal entanglement entropy~\cite{hastings_mahajan2015,lerose2021,carignano_marimon_tagliacozzo2024,foligno2023}, defined through a Schmidt decomposition across a cut in time rather than in space. Although its physical interpretation remains an open question~\cite{doi2023}, it is computationally well defined. For a generic quench, temporal entanglement may grow as rapidly as spatial entanglement, but transverse contraction can still be useful for specific dynamical quantities.

The central quantity we compute throughout is the Loschmidt echo for an infinite, translation-invariant system: the amplitude for a state to return to itself after evolving for a time $T$~\cite{gorin2006}. It measures how quickly the evolved state departs from, and how coherently it returns towards, its initial condition, providing a natural probe of relaxation after a quench. Because the echo is a single space--time amplitude, transverse contraction computes it efficiently, contracting the network directly with a power method~\cite{carignano_tagliacozzo2025}.

At a conformally invariant critical point, the echo also provides direct access to universal data such as the central charge of the underlying \ac{CFT}. In the transverse picture, it is governed by a spatial transfer matrix whose spectrum is predicted by the \ac{CFT} and which becomes effectively unitary at long times---a phenomenon known as emergent dual unitarity~\cite{bertini2019,carignano_tagliacozzo2025}. Both of these predictions have been verified for the critical Ising and three-state Potts chains~\cite{carignano_tagliacozzo2025}, along with the corrections that arise because a simulation reaches only finite times~\cite{boucomas2026}.

This thesis extends that analysis to the non-integrable regime, studying a self-dual quantum spin chain of the axial next-nearest-neighbour Ising (ANNNI) type in the thermodynamic limit: the transverse-field Ising model with an added coupling of strength $p$ between second neighbours~\cite{selke1988,alcaraz2016}. For any $p>0$ the chain is longer-ranged and no longer solvable, yet it remains critical and in the Ising universality class over a wide range of $p$, so the \ac{CFT} predictions provide a clear target for numerical simulations. We prepare the system in the fully polarised paramagnet state, quench it to the critical point, and use the transverse contraction machinery to compute the Loschmidt echo and the generalised temporal entropies, which we compare with the \ac{CFT} predictions. This requires two numerical developments. First, we construct a translation-invariant time-evolution \ac{MPO} that is second-order accurate and treats the \ac{NNN} interactions without requiring prohibitively small time steps. Second, we develop a block power method to resolve the leading transfer-matrix eigenvalues, from which the boundary operator content is obtained.


The thesis is organised as follows. Section~\ref{sec:cft} introduces the Loschmidt echo and the \ac{CFT} predictions for the transfer-matrix spectrum and temporal entropies. Section~\ref{sec:transverse} presents the transverse-contraction framework, Section~\ref{sec:annni} the ANNNI-type model and its equilibrium properties, Section~\ref{sec:mporep} the time-evolution \ac{MPO}, and Section~\ref{sec:numerics} the two iterations used to extract the transfer-matrix spectrum. Section~\ref{sec:results} compares the numerical results with the \ac{CFT} predictions. The appendices provide supporting derivations, implementation details, numerical controls, and additional measurements.

% CFT and universal dynamics
\input{cft}

% Transverse contraction
\input{P3}

% The ANNNI-type Model
\input{P2}

% MPO Representation
\input{P4}

% Numerical methods
\input{numerics}

% Results
\input{results}

%%%%%%%%%%%%%%%%%%%%
% CONCLUSION
%%%%%%%%%%%%%%%%%%%%

\FloatBarrier
\section{Conclusions and Outlook}
\label{sec:conclusions}

This thesis asked whether the conformal signatures that the Loschmidt echo exposes in an integrable chain survive when integrability is broken. We studied a self-dual ANNNI-type chain at its critical point, mapped the echo to a \ac{CFT} on a strip, and read the universal data from the spectrum of the temporal transfer matrix. For the couplings we can reach, $p\le0.5$, all four predictions of Section~\ref{sec:cft:temporal} are confirmed.

The leading eigenvalue keeps an almost constant modulus while its phase winds, which is the emergent dual unitarity of Eq.~\eqref{eq:gapclose}. The curvature of that phase gives a central charge consistent with $c=\tfrac12$ within the resolution of the fits, and the generalised temporal entropy reproduces the predicted chord profile with an imaginary plateau extrapolating to the same value. The phase gaps give the boundary spectrum, where a block of eight temporal states resolves its first seven members at every coupling, following the two conformal towers that the free boundary condition selects. The \ac{NNN} interaction therefore changes the non-universal scales without altering the universal content we can resolve.

Reaching that conclusion required work on several fronts. The model had to be placed in the Ising class independently of the dynamics, which \ac{DMRG} on the ground state does up to $p=1.5$, and the sound velocity had to be measured at each coupling by exact diagonalisation of finite rings. The velocity is not a check but an input, since every prediction converts a phase into universal data through $v$.

The evolution operator required its own construction. An \ac{NNN} interaction cannot be assigned to a single bond of a brick-wall Trotter circuit, and the transverse contraction needs the one-site translation invariance that such a circuit destroys. We therefore represent the Hamiltonian as a finite-state machine and exponentiate that representation directly, using a second-order construction that reproduces every overlapping pair of Hamiltonian terms in a single application. We implemented it in an existing exponential-\ac{MPO} library, and we extract the repeated bulk tensor from a five-site patch so that the channel carrying the \ac{NNN} interaction survives into the temporal column.

The spectral machinery follows from what each observable needs. The entropies and the leading eigenvalue require only the dominant pair, which the single-vector iteration provides, while the boundary content requires the subleading eigenvalues and therefore the block iteration, whose implementation is given in Appendix~\ref{app:blockpm}. The two differ sharply in cost. The single-vector iteration propagates one state on each side and is cheap enough to repeat from many seeds and to reach the longest times in this thesis, whereas the block iteration propagates eight and recombines them at every step, so the boundary content is resolved only up to $T=8$, $6$ and $4.5$ at $p=0.1$, $0.3$ and $0.5$. What can be quoted from either is decided by the numerical controls. We compared the two truncation schemes, varied the bond dimension and the singular-value cutoff, halved the time step, repeated every entropy point from independent random seeds, and tracked the physical branch by continuity rather than by largest modulus. These fix the windows of Table~\ref{tab:cp}.

The point at which the method stops is what we understand least. The measurements establish that the eigenvalues are far more robust than the eigenvectors, since $|\mu_0|$ reproduces across independent seeds at every usable time while the entropies built from the same runs do not, and that the failures set in earlier as the coupling grows. The failure accompanies the left and right states becoming ill-conditioned, with their overlap falling. The controls of Appendix~\ref{app:conditioning} rule out the obvious explanations, since the effect survives exact diagonalisation without any truncation, it is not accounted for by the leading eigenvalues approaching one another, and no single numerical parameter removes it. What property of the \ac{NNN} coupling controls the conditioning therefore remains open, and the windows we report characterise the reach of the present configuration rather than a limit of transverse contraction itself.

Several improvements would extend that reach. Understanding the conditioning would indicate in advance how far a given model can be pushed, and the sound velocity already provides part of that estimate, since the phase gaps scale as $1/(vT)$ and a velocity measured in equilibrium predicts how compressed the spectrum will be before any dynamical calculation is run. The block iteration also leaves room for improvement. Most of its cost lies in recombining the propagated states, and its stopping criterion does not account for the truncation noise floor, so a cheaper and better conditioned implementation would lengthen the accessible windows and make the identification of the physical branch among the Ritz values more reliable, which is the step that the non-normality of the transfer matrix makes delicate. With longer windows, $c(p)$ and $x_1(p)$ could be followed towards the edge of the critical phase, where the equilibrium results remain consistent with Ising criticality but the present dynamical windows are too short.

\clearpage

%%%%%%%%%%%%%%%%%%%%
% BIBLIOGRAPHY
%%%%%%%%%%%%%%%%%%%%
\bibliographystyle{unsrtnat}   % numeric [1]-style, ordered by first appearance
\bibliography{biblio}

\clearpage

%%%%%%%%%%%%%%%%%%%%
% APPENDIX 
%%%%%%%%%%%%%%%%%%%%
\input{appendix}




\end{document}